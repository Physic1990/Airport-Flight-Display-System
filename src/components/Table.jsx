import TableHead from "./TableHead"; // Importing TableHead component
import TableBody from "./TableBody"; // Importing TableBody component

const Table = () => {
    return(
        <table>
            <TableHead/>
            <TableBody/>
        </table>
    )
}

export default Table // Exporting Table component for use in other parts of the application
